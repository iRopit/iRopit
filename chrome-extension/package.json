{
  "name": "chrome-extension",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/popup.js --bundle --outfile=popup/popup.bundle.js --platform=browser --format=iife && esbuild src/sms-window.js --bundle --outfile=popup/sms-window.bundle.js --platform=browser --format=iife",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^12.7.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  }
}
